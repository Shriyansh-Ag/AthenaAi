{
  "name": "intelligence-service",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn app.main:app --reload --port 4003",
    "start": "uvicorn app.main:app --host 0.0.0.0 --port 4003",
    "test": "pytest",
    "lint": "flake8 app",
    "build": "echo 'No build required for Python'"
  }
}
